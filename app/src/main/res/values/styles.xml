<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="background">@color/colorPrimaryDark</item>
        <item name="pinViewStyle">@style/PinWidget.PinView</item>
        <item name="android:layoutDirection">ltr</item>
        <item name="fontFamily">@font/comet_font</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>

    </style>

    <style name="Widget.BottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/comet_font</item>
        <item name="android:textSize">10sp</item>
    </style>

    <style name="SearchViewTheme" parent="Widget.AppCompat.Light.SearchView">
        <item name="android:textColorHint">@color/colorTxtGray</item>
    </style>

    <style name="DefaultNumberPickerTheme" parent="AppTheme">
        <item name="colorControlNormal">@android:color/transparent</item>
        <item name="android:textSize">17sp</item>
        // add if you want to change size
        <item name="android:fontFamily">@font/comet_font</item>
        // add font (this font folder is present in res folder)
        <item name="android:textColorPrimary">@color/colorTxtGray</item>
    </style>
    <style name="normalTxtView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/ui_text_small</item>
    </style>
</resources>